# Leave empty when the frontend and backend are served behind the same domain/proxy.
# Set to a full backend origin for split deployments, for example:
# VITE_BACKEND_URL=https://api.example.com
VITE_BACKEND_URL=
